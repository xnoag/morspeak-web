# Firebase Functions

원래 위치: `Morspeak/홈페이지/functions/` (morspeak-web 과 형제 폴더, git 미관리였다)
2026-09-04 M1 Pro 초기화 이전에 이 저장소로 합쳤다.

배포할 때는 이 폴더를 `functions/` 로 두고 `firebase deploy --only functions` 를 쓴다.
